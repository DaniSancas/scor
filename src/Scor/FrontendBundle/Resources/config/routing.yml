homepage:
    pattern:  /
    defaults: { _controller: FrontendBundle:General:index }

pedir_cita:
    pattern:  /pedir-cita
    defaults: { _controller: FrontendBundle:General:pedirCita }
    requirements:
        _method:  GET

pedir_cita_submit:
    pattern:  /pedir-cita
    defaults: { _controller: FrontendBundle:General:pedirCita }
    requirements:
        _method:  POST

contacto:
    pattern:  /contacto
    defaults: { _controller: FrontendBundle:General:contacto }
    requirements:
        _method:  GET

contacto_submit:
    pattern:  /contacto
    defaults: { _controller: FrontendBundle:General:contacto }
    requirements:
        _method:  POST